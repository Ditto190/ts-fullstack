# Infisical Configuration (CLI-based)
# The Infisical CLI uses GitHub SSO for authentication (session cached locally)

# Project slug (defaults to 'worx-apps')
INFISICAL_PROJECT_SLUG=worx-apps

# Environment (defaults to 'dev')
NODE_ENV=dev

# One-time setup:
# 1. Install CLI: brew install infisical
# 2. Login: infisical login (uses GitHub SSO)
# 3. Load secrets: yarn infisical:load
